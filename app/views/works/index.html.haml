#works.transitions-enabled
    - @works.each do |work|
        .box.panel.panel-default
            = link_to (image_tag work.image.url), work
            %h2= link_to work.title, work
            %p.user
                Submitted by
                = work.user.email
                on
                = work.created_at